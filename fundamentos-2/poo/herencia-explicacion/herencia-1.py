def suma(a,b): # Se crea la función suma con dos parámetros (dos valores).
    return a+b # Retorna la suma de ambos valores.

def suma(a,b,c): # Se crea la función suma con tres parámetros (ters valores).
    return a+b+c # Retorna la suma de los tres valores.

print(suma(1,2,3)) # El resultado es 6, la herencia se lee de abajo hacia arriba.